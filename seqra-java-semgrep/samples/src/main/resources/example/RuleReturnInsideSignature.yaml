rules:
  - id: java.return.chained
    languages:
      - java
    severity: ERROR
    message: Return
    patterns:
      - pattern: |
          $X = src($ARG);
          ...
          return $X;
      - pattern-inside: |
          $RET $METHOD(String $ARG) {
            ...
          }
