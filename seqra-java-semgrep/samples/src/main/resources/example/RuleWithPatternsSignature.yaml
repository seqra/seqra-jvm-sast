rules:
  - id: example-Rule
    languages:
      - java
    severity: ERROR
    message: match example/Rule
    patterns:
      - pattern: |-
          $RET $METHOD(..., String $A, ...) {
              ...
          }
      - pattern: |-
          ...
          sink($A);
          ...
